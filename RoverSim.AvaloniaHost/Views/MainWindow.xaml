<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:RoverSim.AvaloniaHost.Views"
        xmlns:vm="clr-namespace:RoverSim.AvaloniaHost.ViewModels;assembly=RoverSim.AvaloniaHost"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="600" d:DesignHeight="400"
        x:Class="RoverSim.AvaloniaHost.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="RoverSim.AvaloniaHost">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="50"/>
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="150" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <ContentControl Content="{Binding SimulatorSettings}"></ContentControl>
    <ContentControl Grid.Row="1" Content="{Binding AiList}"></ContentControl>
    <ContentControl Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Content="{Binding SimulationList}" />
  </Grid>
</Window>
